# Override that adds a fast file-toggle healthcheck to `web`.
services:
  web:
    healthcheck:
      test: test ! -f /tmp/drain
      interval: 1s
      timeout: 2s
      retries: 2
      start_period: 1s
